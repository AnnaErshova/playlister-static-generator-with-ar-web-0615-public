<!DOCTYPE html>
<html>
  <head>
    <title>Songs</title>
  </head>
  
  <body>
    <ul>
      <% @songs.each do |song| %>
      <li> 
        <a href="<%= song.to_slug %>.html"> <%=song.name %> </a> 
      </li>
      <% end %> 
    </ul>
  </body>
</html>