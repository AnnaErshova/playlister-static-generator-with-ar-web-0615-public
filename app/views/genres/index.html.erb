<!DOCTYPE html>
<html>
  <head>
    <title>Genres</title>
  </head>
  
  <body>
    <ul>
      <% @genres.each do |genre| %>
      <li> 
        <a href="<%= genre.to_slug %>.html"> <%=genre.name %> </a> 
      </li>
      <% end %> 
    </ul>
  </body>
</html>